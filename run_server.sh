#!/usr/bin/env bash
# ===========================================================
# 방짝 AI 추론 서버 실행 스크립트 (학과 서버용)
# 사용법:
#   chmod +x run_server.sh
#   ./run_server.sh
# 또는 백그라운드로:
#   nohup ./run_server.sh > server.log 2>&1 &
# ===========================================================

# ⚠️ 본인 팀 포트로 변경하세요 (예: 60001~60008 / 65001~65006)
PORT=60007

# 프로젝트 루트로 이동
cd "$(dirname "$0")"

# 서버 실행
exec uvicorn api.app:app --host 0.0.0.0 --port "$PORT"
